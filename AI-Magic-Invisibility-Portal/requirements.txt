# AI Magic Invisibility Portal - Dependencies
# Python 3.9 - 3.14 supported.

opencv-python>=4.8.0
# MediaPipe Tasks API (HandLandmarker) is used; the legacy mp.solutions.hands
# API was removed in mediapipe 0.11+ and has no Python 3.12+ wheels. Install
# the newest version your Python supports (Python 3.14 needs mediapipe >= 0.10.18).
mediapipe>=0.10.18
numpy>=1.24.0
